Config = {}

Config.SyncLowerPerms = true --Set to false if you don't want to sync permissions lower than a user already has.

Config.Log = true --Set to false to disable logging the syncing of permissions to the server console